---
"react-doctor": patch
"eslint-plugin-react-doctor": patch
---

Honor `diagnose(..., { lint: false })` by using the no-op lint layer in the programmatic API, and align ESLint rule metadata with the canonical React Doctor rule titles and docs URLs.
