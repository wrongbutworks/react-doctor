---
"react-doctor": patch
---

Clean up config/report drift: `ignore.overrides` now honors legacy rule aliases, JSON report types include `baselineDegraded`, and the `why` command no longer references the removed `--explain` flag in its project-selection error.
